agents/memory/
project/
# project/state.db is committed as an empty/seed default so the repo ships a
# working database, but its per-run mutations (tests / `gabbe init`) must NOT be
# committed. It is tracked-but-ignored: contributors should run
#   git update-index --skip-worktree project/state.db
# to keep the seed pristine in diffs.
project/state.db

__pycache__
*.pyc
.venv
venv
gabbe.egg-info
dist/
build/
.coverage
.pytest_cache
.env
.env.local
.env.*.local
.git.bak/
.ruff_cache
.mypy_cache
env
GABBE-v1.0-MASTER-PLAN.md
